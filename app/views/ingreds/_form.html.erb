<%= form_with(model: ingred, local: true) do |form| %>
  <% if ingred.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ingred.errors.count, "error") %> prohibited this ingred from being saved:</h2>

      <ul>
      <% ingred.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :recipe_id %>
    <%= form.number_field :recipe_id, id: :ingred_recipe_id %>
  </div>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.number_field :item_id, id: :ingred_item_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
